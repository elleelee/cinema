<div class="container">
<h4>Where</h4>
<%= simple_form_for :search, url: root_path, method: :get do |f| %>
  <div class="search-form-control form-group">
    <input class="form-control string required" placeholder = "e.g. London" type="text" name="search[query]" id="search_query" />
<!--     <button name="button" type="submit" class="btn btn-flat">
      <i class="fas fa-search"></i> Search
    </button> -->
  </div>
<% end %>
  <%= simple_form_for :search, method: "get" do |f| %>
    <div class="row" style="margin: 0">
      <%= f.input :From, as: :string, placeholder: "dd.mm.yy", required: false, input_html: {class: "datepicker search-box"} %>
      <%= f.input :To, as: :string, placeholder: "dd.mm.yy", required: false, input_html: {class: "datepicker search-box"} %>
    </div>
  <!--     <%= f.button :submit, "Go", class: "btn btn-primary" %>
   -->  <% end %>
  <div class="button-holder"><button type="button" class="btn btn-danger"><a href="/events">Search</a></button></div>
</div>

