<div class="images">
  <img src="" alt="">
  <img src="" alt="">
  <%= @event.photo %>
</div>

<div class="container">
  <div class="left-showbox">
    <%= @event.name %>
    Showing: <%= @event.movie %>
    @<%= @event.address %>
  </div>
  <div class="right-showbox">

  </div>
</div>

<%= @event.description %>
<%= @event.date %>
<%= @event.ticket_price %>
<%= @event.venue_type %>
<%= @event.capacity %>

<%= link_to 'Update Event', edit_event_path(@event) %>
<%= link_to 'Delete Event', event_path(@event), method: :delete, data: { confirm: "Delete #{@event.name}?" }  %>
<%= link_to 'Back to Events', events_path %>

